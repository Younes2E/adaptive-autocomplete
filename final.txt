Information Retrieval : 
Trie
noisy channel - Damerau-Levenshtein avec score
exemple : pour "acress" : tester [a-z]acress, [a-z]cress, a[a-z]ress, ac[a-z]ess, acr[a-z]ss, acre[a-z]s, acres[a-z], acress[a-z] -> insertion, substitution ...

fonctionnalité : add to dictionnary -> scan le text entier et add le nbr d'occ : garder dans le cache les candidat pour ne pas recalculer les candidats à chaque fois ?



confusion matrix -> 26*26 (plus avec accents etc), nb of times a letter was substituted for another


proposer les mots qui vont dans le contexte de la phrase
partir de tous les candidats,
modele pré entrainé avec des textes (flandes gitlab // wikipedia hugging face)
qui s'adapte -> continual learning


save dans un json l'état du trie pour la prochaine session

(BERTScore)

modeliser faute accents (é, à, è) avec la matrice de confusion

////////////////////////////////////////////////////////////////////////////////////////////////////


trie.py
Class Trie :
	attr : freq, nb_used, nb_used_trie (pour compute nb_used / nb_used_trie -- additionner en depilant la pile d'appel)
	fonctions : add, remove, use(quand l'utilisateur entre un mot), get (renvoie n mots du sous arbre de profondeur <= p), get_noise (renvoie n mots du sous arbre dont la edit distance <= d)


Trie.use(w)
si w est dans les mots proposé on fait trie.use(w), sinon on attend que le user fasse 'add w to dicionnary' pour use, qui va ajouter le mot dans le trie puis le use
























