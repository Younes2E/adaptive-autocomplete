Information Retrieval : 
Trie
noisy channel - Damerau-Levenshtein avec score

fonctionnalité : add to dictionnary -> scan le text entier et add le nbr d'occ : garder dans le cache les candidat pour ne pas recalculer les candidats à chaque fois ?



confusion matrix -> 26*26 (plus avec accents etc), nb of times a letter was substituted for another


proposer les mots qui vont dans le contexte de la phrase
partir de tous les candidats,
modele pré entrainé avec des textes (flandes gitlab // wikipedia hugging face)
qui s'adapte -> continual learning


save dans un json l'état du trie pour la prochaine session

(BERTScore)

modeliser faute accents (é, à, è) avec la matrice de confusion

////////////////////////////////////////////////////////////////////////////////////////////////////


trie.py
Class Trie :
	attr : freq, nb_used, nb_used_trie (pour compute nb_used / nb_used_trie -- additionner en depilant la pile d'appel)
	fonctions : add, remove, use(quand l'utilisateur entre un mot), get (renvoie n mots du sous arbre de profondeur <= p), get_noise (renvoie n mots du sous arbre dont la edit distance <= d)


Trie.use(w)
si w est dans les mots proposé on fait trie.use(w), sinon on attend que le user fasse 'add w to dicionnary' pour use, qui va ajouter le mot dans le trie puis le use

Trie.get_noise(word)
recupere les mots a une edit distance de 2, puis recupere leurs completions possibles (avec score) 


confusion.py
Class Confusion:
4 tables
constructeur importe les 4 csv et les met dans 4 dataframe
(4 csv crées préalablement)
4 fonctions : del, rev, add, subst
unigram et bigram count

MRR (Mean Reciprocal Rank)

JSON :
Enregistrer les mots plutot que les nodes, car nb_node = nb_prefixes de chaque mot et il existe plus de prefixe que de mots au pire cas


amelioration possibles : 
matrice confusion modelisent l'erreur au clavier, il faudrait ajouter une dimension phonetique aussi -> les erreurs ne sont plus indé -> autre que Naive Bayes

rapport : 
montrer la distribution gaussienne sur le clavier, chaque touche a une gaussienne centrée en sa position

get_noise("tra")
tr|a -> subst = del, ça fait la meme chose donc on ne peut pas delete quand on est sur la derniere lettre
sur les |distance| dernieres lettre de w, delete est inutile
references : correction de pov de correction // nous : pov typo (word est consideré comme la typo)
Dans le modèle Noisy Channel, on considère que le canal a "appliqué" une transformation sur le mot correct word pour produire l'erreur x,
On fait le contraire, on part de la typo pour trouver les mots valides qui aurait pu etre deformés -> Plus rapide
















